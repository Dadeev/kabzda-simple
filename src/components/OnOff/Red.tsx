import React from 'react';

const Red = () => {
    return (
        <div>
            <button>on</button>
            <button style={{color: 'red'}}>off</button>
            <button style={{color: 'red'}}>circle</button>
        </div>
    );
};

export default Red;